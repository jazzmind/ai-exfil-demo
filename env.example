# Upstash Redis credentials (free tier works fine for this demo)
# Get these from https://console.upstash.com/ after creating a Redis database
# OR they will be auto-injected if you connect a Vercel KV store to this project
KV_REST_API_URL=
KV_REST_API_TOKEN=
